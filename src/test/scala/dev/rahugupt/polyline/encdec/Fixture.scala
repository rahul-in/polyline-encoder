package dev.rahugupt.polyline.encdec

import dev.rahugupt.polyline.domain.Point

object Fixture {

  val points = Seq(Point(Math.PI, Math.PI * 2, 1), Point(Math.PI * 1.1, Math.PI * 2 * 1.1, 2), Point(Math.PI * 1.2, Math.PI * 2 * 1.2, 1))
}
